# Build artifacts
/target/
rust_out/

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Debugging
*.pdb
core
core.*

# Coverage
lcov.info
tarpaulin-report.html
tarpaulin-report.json
coverage/

# Benchmark output (criterion generates large HTML reports)
criterion/

# Fuzz artifacts
fuzz/target/
fuzz/corpus/
fuzz/artifacts/

# Proptest persistence (regressions are committed, but not other state)
proptest-regressions/

# cargo-vet downloads
supply-chain/.cache/

# Cyrius dep-resolution caches (regenerated by `cyrius deps` next to each
# vendored bundle; the bundles themselves ARE committed). dist/*.deps is NOT
# ignored — that sidecar is part of the published dist contract.
lib/*.cyr.f
lib/*.cyr.syms

# Claude Code
.claude/

# Environment / secrets
.env
.env.*
